import { height } from '@mui/system';
import React from 'react';
import bg from '../../../images/dashboardBg.jpg'

const DashboardHome = () => {
    const style = {
        background: `url(${bg})`,
        width: '100%',
        height: '400px'
    }
    return (
        <div style={style}>

        </div>
    );
};

export default DashboardHome;